# The test files in the repo are encrypted. Only those trusted by the maintainer will gain access to the decryption key
tar -cvf - files/ | gzip | openssl enc -aes-256-cbc -pbkdf2 -salt -out files.tar.gz